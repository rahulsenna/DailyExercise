<Window x:Class="CustomMenuBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomMenuBar"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="#222222"
        WindowStyle="None"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        >
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="0" CaptionHeight="0" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="93*"/>
                <ColumnDefinition Width="7*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Menu FontSize="12" Background="Transparent" Foreground="White">
                    <MenuItem Header="File">
                        <MenuItem Header="New">
                            <MenuItem Header="Project / Solution" />
                        </MenuItem>
                    </MenuItem>
                        <MenuItem Header="Edit" />
                    <MenuItem Header="View" />
                </Menu>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Click="MinimizeButton_Click" Width="20" Height="20" Content="🗕" BorderBrush="Transparent" Foreground="White"  Background="Transparent" />
                <Button Click="MaximizeButton_Click" Width="20" Height="20" Content="🗖" BorderBrush="Transparent" Foreground="White" Background="Transparent" />
                <Button Click="CloseButton_Click" Width="20" Height="20" Content="X" BorderBrush="Transparent" Foreground="White" Background="Transparent" />

            </StackPanel>

        </Grid>

    </Grid>
</Window>
